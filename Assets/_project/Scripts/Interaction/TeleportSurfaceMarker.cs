using UnityEngine;

namespace MemoryGarden.Interaction
{
    /// <summary>Explicitly marks only geometry that is safe for teleport landing.</summary>
    [DisallowMultipleComponent]
    public sealed class TeleportSurfaceMarker : MonoBehaviour
    {
    }
}
